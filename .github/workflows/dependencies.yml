name: Dependencies

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Packages/Dependencies/**/*
      - .github/workflows/dependencies.yml
  pull_request:
    paths:
      - Packages/Dependencies/**/*
      - .github/workflows/dependencies.yml

env:
  PLATFORM_IOS: iOS Simulator,name=iPhone 15 Pro,OS=17.0.1

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - uses: irgaly/xcode-cache@v1
        with:
          key: dependencies-xcode-cache-deriveddata-${{ github.workflow }}-${{ github.sha }}
          restore-keys: dependencies-xcode-cache-deriveddata-${{ github.workflow }}-
      - run: xcodebuild build -workspace Dependencies.xcworkspace -scheme "DependenciesLibrary" -skipMacroValidation -sdk iphonesimulator | xcpretty

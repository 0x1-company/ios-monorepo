# import PictureSlider from './PictureSlider.graphql';

fragment MatchGrid on Match {
  id
  createdAt
  isRead
  targetUser {
    id
    displayName
    externalProductUrl
    ...PictureSlider
  }
}

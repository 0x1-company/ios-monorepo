# import PictureSlider from './PictureSlider.graphql';

fragment MatchGrid on Match {
  id
  createdAt
  isRead
  targetUser {
    id
    displayName
    berealUsername
    externalProductUrl
    ...PictureSlider
  }
}

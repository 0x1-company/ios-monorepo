# import PictureSlider from './PictureSlider.graphql'

fragment UserInternal on User {
  id
  berealUsername
  externalProductUrl
  gender
  status
  images {
    id
    imageUrl
  }
  shortComment {
    id
    body
    status
  }
  ...PictureSlider
}

# import PictureSlider from './PictureSlider.graphql'

fragment UserInternal on User {
  id
  displayName
  berealUsername
  externalProductUrl
  gender
  status
  images {
    id
    imageUrl
  }
  shortComment {
    id
    body
    status
  }
  ...PictureSlider
}

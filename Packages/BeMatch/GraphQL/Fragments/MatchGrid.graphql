# import PictureSliderImage from './PictureSliderImage.graphql';

fragment MatchGrid on Match {
  id
  createdAt
  isRead
  targetUser {
    id
    berealUsername
    images {
      ...PictureSliderImage
    }
  }
}

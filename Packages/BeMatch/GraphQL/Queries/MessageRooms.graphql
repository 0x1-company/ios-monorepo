query MessageRooms($first: Int!, $after: String) {
  messageRooms(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        updatedAt
      }
    }
  }
}